*Below is a list of all of the information about how to take user input within spark.*

All of the examples below assume you already have a cell at the top with:

```python
!pip install --upgrade schulich-ignite
import spark
%reload_ext spark
```